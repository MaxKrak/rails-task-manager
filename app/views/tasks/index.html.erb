 <h1>My Tasks</h1>
 <% @tasks.each do |task| %>
  <div>
    <input type="checkbox" id="<%= task.title %>" <%= "checked" if task.completed %>>
    <label for="<%= task.title %>"><%= task.title %></label>
  </div>
<% end %>
